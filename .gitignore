# dependencies
node_modules

# build output
dist
.next
out

# env / secrets
.env
.env.local
.env.*.local
env.config

# runtime artifacts
uploads/*
public/audio/*
!public/audio/.gitkeep

# test / coverage
coverage

# eval output
lib/eval/results/*
!lib/eval/results/.gitkeep

# os
.DS_Store

# local test inputs (not part of the repo)
sample_papers/

# generated episode output
*.episode.json

# local TTS runtime and voice models (large, fetched on demand)
.venv-tts/
.voices/

# ASR models (large, fetched on demand)
.models/

# Next.js
.next/
next-env.d.ts
AGENTS.md

# study ledger (personal)
learning.json
learning.json.tmp

# demo papers (fetched from the manifest, not redistributed here)
demo/papers/

# saved episodes (the library)
data/episodes/
